<t:NTTabPage	xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
		xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
		xmlns:t="clr-namespace:NinjaTrader.Gui.Tools;assembly=NinjaTrader.Gui"          
		xmlns:system="clr-namespace:System;assembly=mscorlib" 
        x:Class="NinjaTraderAddOnProject.AddOnPage" >
    <Grid Background="Transparent">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="100"/>
        </Grid.RowDefinitions>

        <!-- Configuration Section -->
        <StackPanel Grid.Row="0" Margin="10">
            <TextBlock Text="Template Source Folder:" Style="{StaticResource FontLabel}"/>
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBox x:Name="txtSourceFolder" Margin="0,5,5,5"/>
                <Button x:Name="btnBrowseSource" Content="..." Width="30" Grid.Column="1" Click="btnBrowseSource_Click"/>
            </Grid>

            <TextBlock Text="Export Destination Folder:" Style="{StaticResource FontLabel}" Margin="0,10,0,0"/>
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBox x:Name="txtDestFolder" Margin="0,5,5,5"/>
                <Button x:Name="btnBrowseDest" Content="..." Width="30" Grid.Column="1" Click="btnBrowseDest_Click"/>
            </Grid>
        </StackPanel>

        <!-- Controls Section -->
        <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Center" Margin="10">
            <Button x:Name="btnStartBatch" Content="Start Batch" Width="100" Margin="5" Click="btnStartBatch_Click"/>
            <Button x:Name="btnStopBatch" Content="Stop Batch" Width="100" Margin="5" IsEnabled="False" Click="btnStopBatch_Click"/>
        </StackPanel>

        <!-- Template List Section -->
        <GroupBox Grid.Row="2" Header="Templates Found" Margin="10">
            <ListBox x:Name="lstTemplates"/>
        </GroupBox>

        <!-- Progress Section -->
        <ProgressBar x:Name="pbProgress" Grid.Row="3" Height="20" Margin="10,0,10,10"/>

        <!-- Log Section -->
        <TextBox x:Name="outputBox" Grid.Row="4" Margin="10,0,10,10" IsReadOnly="True" TextWrapping="Wrap" VerticalScrollBarVisibility="Auto" Background="#111" Foreground="#EEE"/>
    </Grid>
</t:NTTabPage>
